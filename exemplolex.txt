Program teste;
	Integer x, y , zz1; 
	real i1, i2;
	String l1;
Begin
	Begin
		i1 := i2 + 10 - 5;
                x := vif; {esse comentario tem que ser ignorado}
		if (( x> 20) and (y < 10)) then
			All (l1);
                while(x < 4) do
                    y := 2 + y;
                repeat
                    all(l1);
                until(x < y);
	end;
end.
